# 测试配置文件 - 仅包含 3 条命令用于快速测试
# 验证序号自动生成功能和协议智能过滤

tshark -r {INPUT} -q -z io,phs::protocol-hierarchy.md::all
tshark -r {INPUT} -q -z conv,ip::ipv4-conversations.md::ip
tshark -r {INPUT} -q -z dns,tree::dns-general.md::dns

